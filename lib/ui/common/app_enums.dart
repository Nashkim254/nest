enum DiscoverableType {
  all,
  people,
  organizations,
}

enum SettingsType {
  everyone,
  friends,
  none,
}

enum MessageType {
  sent,
  received,
}

enum ImageSourceType { camera, gallery, multiple }

enum ContentType {
  fyp,
  upcoming,
  following,
}
